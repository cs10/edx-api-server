// Eventually this is where the logic will live to handle auth.

// First check that process.env.REQUIRE_AUTH_ALWAYS=false
// Then if false check if a practicular route is public
// Then check headers for auth...
// TODO: seems like there's a package to help with this..
